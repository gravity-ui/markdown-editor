import {
    Math,
    type MathOptions,
} from '@gravity-ui/markdown-editor/_/extensions/additional/Math/index.js';

export const LatexExtension = Math;
export type LatexExtensionOptions = MathOptions;

export {LatexNodeName, LatexNodeType} from './specs';
